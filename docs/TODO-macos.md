# macOS TODO

- ✅ Add a proper macOS program icon and wire it into the app bundle.
- ✅ Record the relevant user and program paths in the macOS ADR so the setup assumptions are explicit.
- Implement path handling for macOS
- Update the macOS-related documentation.
- Add `pandoc` as a documented macOS prerequisite for help generation.
- Investigate why the HTML help templates are not found on macOS; likely a path handling issue.
- Review fullscreen behavior on macOS and align it with the expected app behavior.
- Add proper macOS app packaging.
- Replace the temporary null backend with a real macOS gamepad implementation.
